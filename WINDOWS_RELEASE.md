# انتشار نسخهٔ ویندوز آوای گندم

این پروژه با Electron به فایل نصب ویندوز تبدیل می‌شود. کد صفحه با React نوشته شده و در نسخهٔ نصب‌شده نیز بدون سرور اجرا می‌شود.

## ساخت روی رایانهٔ خودت

در پوشهٔ پروژه:

```powershell
npm ci
npm run build
npm run dist:win -- --publish never
```

فایل نصب در پوشهٔ `release/` با نامی مانند `AvayeGandom-Setup-0.5.1-x64.exe` ساخته می‌شود.

## دانلود از GitHub

۱. کل پروژه را، همراه با `.github/workflows/windows-release.yml` و `package-lock.json`، در یک مخزن GitHub قرار بده.

۲. نسخهٔ `package.json` و برچسب Git باید یکسان باشند. برای نسخهٔ کنونی، برچسب `v0.5.1` بساز و به GitHub بفرست:

```powershell
git tag v0.5.1
git push origin v0.5.1
```

۳. در تب **Actions** منتظر تمام شدن «Windows release» بمان. فایل نصب در بخش **Releases** همان مخزن برای دانلود قرار می‌گیرد. فرستادن کد بدون برچسب نسخه فایل نصب منتشر نمی‌کند.

نسخهٔ نصب‌شده داده‌ها را در فایل SQLite داخل پوشهٔ دادهٔ کاربر ویندوز نگه می‌دارد. نسخهٔ مرورگر فقط نمایشی است و داده‌ها را نگه نمی‌دارد. برای حساب‌های واقعی فروشگاه، بخش‌های حسابداری، کنترل و بازبینی بیشتری لازم است.

## آیکون و هشدار SmartScreen

آیکون چنداندازهٔ ویندوز در `build/icon.ico` قرار دارد و برای فایل برنامه، نصب‌کننده و سربرگ نصب استفاده می‌شود.

هشدار «Windows protected your PC» مربوط به ناشر ناشناخته است و با آیکون رفع نمی‌شود. برای حذف این هشدار باید گواهی معتبر **Code Signing** تهیه شود. فایل گواهی و رمز آن را در GitHub Actions Secrets با نام‌های `WIN_CSC_LINK` و `WIN_CSC_KEY_PASSWORD` قرار دهید. گردش‌کار انتشار از این دو مقدار برای امضای فایل اجرایی استفاده می‌کند. ممکن است SmartScreen برای گواهی‌های تازه تا شکل‌گیری اعتبار ناشر همچنان هشدار نشان دهد.
